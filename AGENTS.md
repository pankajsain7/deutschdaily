# DeutschDaily Agent Guidelines & Rules

## Backup Management
- **Keep Only the Latest Backup**: When new preset backups are added or updated in `src/backup-presets.js`, retain only the single latest backup snapshot in `PREVIOUS_BACKUPS`. Older backup snapshots must be removed rather than accumulated.
